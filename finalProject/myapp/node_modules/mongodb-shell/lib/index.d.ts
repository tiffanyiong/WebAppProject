/// <reference types="node" />
import { Readable } from 'stream';
export interface Logger {
    debug: (message?: any) => void;
    info: (message?: any) => void;
    warn: (message?: any) => void;
    error: (message?: any) => void;
}
export interface CommandIn {
    in: string;
}
export declare class MongoShell {
    private workerId;
    private logger;
    stdout: Readable;
    private mongoUri;
    private mongo;
    constructor(workerId: string, mongoUri: string, logger?: Logger);
    private bindStdout;
    init(): Promise<void>;
    destroy(): Promise<void>;
    sendCommand(command: CommandIn): void;
}
